---
import type { Lang } from '../data/i18n';
import SubpageHeader from './SubpageHeader.astro';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const isEs = lang === 'es';
---
<div class="subpage">
  <SubpageHeader lang={lang} basePath="/about" />
  <main>
    <p class="label">{isEs ? 'Quiénes Somos' : 'About Us'}</p>
    <h1>{isEs ? 'Sobre Brisa Padel' : 'About Brisa Padel'}</h1>
    <p class="intro">
      {isEs
        ? 'Brisa Padel es una plataforma privada de matchmaking de pádel para el sur de Florida. Creamos partidos equilibrados de 4 jugadores cada semana, sin el caos de coordinación manual.'
        : 'Brisa Padel is a private-membership padel matchmaking platform serving South Florida. We build balanced, fun, four-player matches every week without the manual back-and-forth.'}
    </p>

    <section>
      <h2>{isEs ? 'Por qué existimos' : 'Why we exist'}</h2>
      <p>
        {isEs
          ? 'Entre 2023 y 2026 el pádel creció rápidamente en Florida. El problema se repitió en todos lados: conseguir 4 jugadores bien nivelados y disponibles al mismo tiempo es difícil. Brisa Padel se creó para resolver eso.'
          : 'Padel exploded across South Florida between 2023 and 2026. The same issue kept repeating: getting four compatible players on court at the same time is hard. We built Brisa Padel to solve that.'}
      </p>
    </section>

    <section>
      <h2>{isEs ? 'Qué hacemos' : 'What we do'}</h2>
      <p>{isEs ? 'No somos un club ni vendemos canchas. Coordinamos partidos entre los clubes donde ya juegas.' : "We don't own courts and we don't replace your club. We coordinate matches across the clubs you already play at."}</p>
      <ul>
        <li>{isEs ? 'Balance de nivel (WPR)' : 'Skill balance (WPR)'}</li>
        <li>{isEs ? 'Preferencia de lado (izquierda/derecha/ambos)' : 'Side preference (left/right/both)'}</li>
        <li>{isEs ? 'Estilo de juego y tipo de partido' : 'Playing style and match type'}</li>
        <li>{isEs ? 'Preferencia de clubes y disponibilidad semanal' : 'Club preferences and weekly availability'}</li>
      </ul>
    </section>

    <section>
      <h2>{isEs ? 'Cómo somos diferentes' : "How we're different"}</h2>
      <ul>
        <li><strong>WhatsApp-first.</strong> {isEs ? 'Sin app nueva ni contraseñas extra.' : 'No separate app or extra login flow.'}</li>
        <li><strong>AI-powered.</strong> {isEs ? 'Entiende lenguaje natural en inglés y español.' : 'Availability parsing works in English and Spanish.'}</li>
        <li><strong>{isEs ? 'Privado y curado.' : 'Private and curated.'}</strong> {isEs ? 'Cada aplicación se revisa manualmente.' : 'Every application is reviewed.'}</li>
        <li><strong>{isEs ? 'Cross-club.' : 'Cross-club.'}</strong> {isEs ? 'Jugás donde ya jugás.' : 'You keep playing where you already play.'}</li>
      </ul>
    </section>

    <section>
      <h2>{isEs ? 'Cobertura' : 'Service area'}</h2>
      <ul>
        <li>Miami-Dade County</li>
        <li>Broward County</li>
        <li>Palm Beach County</li>
        <li>Central Florida (Orlando metro)</li>
      </ul>
    </section>

    <section>
      <h2>{isEs ? 'Contacto' : 'Get in touch'}</h2>
      <p>{isEs ? 'Si eres jugador, coach o club y quieres colaborar, escríbenos.' : "If you're a player, coach, or club interested in working with us, contact us."}</p>
      <a class="cta" href={isEs ? '/es/contact' : '/contact'}>{isEs ? 'Contactar' : 'Contact us'}</a>
    </section>
  </main>
</div>

<style>
  .subpage { min-height: 100vh; background: #faf8f5; }
  main { max-width: 760px; margin: 0 auto; padding: 3.5rem 1.5rem; }
  .label { margin: 0 0 .75rem; font-size: .72rem; text-transform: uppercase; letter-spacing: .16em; color: #8b8178; font-weight: 700; }
  h1 { font-family:"Cormorant Garamond", serif; font-size: clamp(2rem, 5vw, 3.3rem); margin: 0 0 .8rem; color:#1c1917; }
  .intro { color:#57534e; line-height:1.7; margin: 0 0 1.5rem; }
  section { margin-top: 1.6rem; }
  h2 { font-family:"Cormorant Garamond", serif; border-left:2px solid var(--brand-orange); padding-left:.8rem; color:#1c1917; margin:0 0 .7rem; font-size:1.42rem; }
  p { margin: 0; color:#57534e; line-height:1.65; }
  ul { margin: .7rem 0 0; padding-left: 1.2rem; color:#57534e; line-height:1.65; }
  .cta { display:inline-block; margin-top: .9rem; background:var(--brand-orange); color:#fff; padding:.7rem 1.3rem; text-decoration:none; text-transform:uppercase; font-size:.75rem; letter-spacing:.14em; font-weight:700; }
</style>
